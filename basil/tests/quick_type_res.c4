// minimal test for type resolution
#include <stdio.h>

// named multi-return with basic types only
fn1(int x) int a = -1, int b = 0 {
    if (b == 0) return;
    a = x;
    b = 1;
}

main () int {
    v1, v2 := fn1(5);
    printf("%d %d\n", v1, v2);
    return 0;
}
