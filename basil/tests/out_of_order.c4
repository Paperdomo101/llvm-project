#include <stdio.h>

main () int {
    int r = 10::add(4);
    r::print();
    if (r != 14) {
        printf("FAIL: expected 14, got %d\n", r);
        return 1;
    }
    return 0;
}

add_ints / add (int a, b) int {
    return a + b;
}

print_int / print (int i) {
    printf("%d\n", i);
}
