// tests/static_global_type_inferred_runtime.c4
#include <stdio.h>

static markers := 64;

main () int {
    if (markers != 64) return 1;

    printf("Static global type inferred runtime test passed!\n");
    return 0;
}
