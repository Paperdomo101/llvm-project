#include <stdio.h>
#include <stdlib.h>

int main() {
    int size = 21;
    [size] char buffer = {};
    for (int i = 0; i < 21; ++i) {
        printf("%d ", buffer[i]);
    }
    printf("\n");
    return 0;
}
